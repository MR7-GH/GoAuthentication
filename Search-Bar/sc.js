const search = document.querySelector('.search');
const button = document.querySelector('.animated-button');

search.addEventListener('click', () => {
  button.classList.add('show'); // Add the 'show' class to display the button
});
